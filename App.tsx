# --- SERVER SIDE (Private) ---
# Note: In AI Studio Build, GEMINI_API_KEY is injected automatically.
# On Vercel, you must add it in Settings > Environment Variables.
GEMINI_API_KEY=
GMAIL_USER=pnk.pd.stock@gmail.com
GMAIL_APP_PASSWORD=

# --- CLIENT SIDE (Public) ---
# Vite prefixes with VITE_ to expose them to the browser.
# These are safe to be public IF Supabase RLS is enabled.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
